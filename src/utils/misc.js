export const randBetween = (min, max) => Math.random() * (max - min) + min
export const isEven = (n) => n % 2 == 0;
export const isInRange = (min, max, number) => number > min && number <= max ? true : false

